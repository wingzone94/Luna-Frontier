# Luna interactive — 検証記録

## 1.3.18（2026-09-24）

- cybernode.localの5系列記事でホバー中の円弧を強調し、項目A・系列1・値50・割合50%のツールチップが指している円の外側に出ることを目視確認。棒・折れ線でもホバーの系列は1つ。
- 非表示の凡例は文字と枠で示し、長い系列名は2行で揃える。1280pxと320pxの実表示を確認。
- グラフ切り替え時は新しいCanvasやWebPを先に描き、切り替え中に空の描画フレームが発生しないことをLocalの自動テストで確認。
- `node luna-interactive/tests/local-regression.cjs`で、320pxの横はみ出し・2＋2＋1配置・5円の同一直径、均等データの120°、系列・項目の選択保持、WebP画像の内容、円・棒・折れ線のホバーを確認。JavaScriptエラーなし。PHPとJavaScriptの構文検査も通過。
- テーマ本体は変更しない。

## 1.3.16（2026-09-24）

- 1.3.15は公開側でChart.jsのanimationを無効にし、作成直後の即時更新でも動きを止めていた。動的表示では描画アニメーションを有効にし、静的WebPは即時描画のまま維持。
- cybernode.localの5系列記事で、静的から動的に戻した直後と約1秒後の円の描画画像が異なることを確認。系列の非表示・再表示、項目Aの解除後もCanvasが5個のままで、表示中のB・Cの割合に更新。エラー表示なし。
- 静的表示に戻すと5枚のWebPを確認。公開画面では1.3.16のview.jsを読み込み。JavaScript・PHPの構文検査を通過。
- テーマ本体は変更しない。

## 1.3.15（2026-09-24）

- cybernode.localの5系列記事で320px幅を確認。円は2＋2＋1に並び、全体のscrollWidth 305pxはviewport 320px以内。390px幅は直径128pxの2＋2＋1で、scrollWidth 375pxはviewport 390px以内。
- 動的Canvasの5系列すべてに割合を含む読み上げ名を確認。系列を非表示にしてもCanvasは5個のまま、項目Aを解除すると残るB・Cから割合を再計算。全項目を解除した状態からの再表示も確認。
- 静的ビューのWebP画像と代替テキスト、棒形式へ切り替えた後の静的モード・系列選択の保持を確認。サーバー出力の初期SVGにも割合を含むaria-labelを確認。PHP構文とJavaScript構文を確認。
- 編集プレビューのコードは構文確認済み。ブラウザーの編集画面は認証が必要なため実表示を未確認。
- テーマ本体のファイルは変更しない。

## 1.3.14（2026-09-24）

- cybernode.localの5系列・2系列・1系列の円を確認。PC本文幅で5系列は横一列・直径128px、2系列は180px、1系列は200px。系列名ボタンで非表示・再表示でき、項目選択と静的WebPは形式切り替え後も保持。
- 390px幅は2＋2＋1、320px幅は1列で横はみ出しなし。テーマのライト・ダーク両方で表示と再描画を確認。PCの円にホバーして項目A=50%を確認。
- Gutenbergで5系列のプレビューと「系列を追加」の無効状態、静的WebPプレビューを確認。JavaScriptなしのHTMLをブラウザーで開き、5個の初期SVGと共通凡例をPC・390px幅で確認。
- テーマ本体のファイルは変更しない。

以下は過去バージョンの検証履歴です。

## 1.3.13（2026-09-24）

- 円グラフを系列別の塗りつぶし円へ変更。1系列ごとにChart.jsの円を生成し、初期SVGも同じ構成にした。新規・再編集時に円を許可する場合の系列上限は5、棒・折れ線のみは20。既存の6系列以上の公開データは切り捨てない。
- cybernode.local の5系列テスト記事で5個の円と中央揃えの折り返しを確認。2系列テスト記事でも2個の円を確認。390px幅では1列で横はみ出しなし。ダーク配色でも5個の円とエラーなしを確認。
- 5系列の静的表示で5枚のWebPを確認。系列と項目の非表示状態は棒・折れ線との形式切り替え後も保持。サーバー出力は5系列分の初期SVGと凡例を確認。
- `php -l`、`node --check` を通過。Gutenbergの編集画面はブラウザー接続のタイムアウトにより実表示を未確認。

以下は過去バージョンの検証履歴です。

## 1.3.12（2026-09-24）

- 複数系列の円グラフで、外周と内周の面積を同じにするリング境界を使用。等しい割合の項目は同じ面積となる。JavaScriptの2・3・20系列の幾何計算では面積差が浮動小数点誤差の範囲に収まることを確認。
- 動的CanvasのChart.js dataset weightと、JavaScript無効時の初期SVGの半径計算を同じ式に合わせた。
- cybernode.localの均等データ（各系列A・B・Cとも10）で、2系列と3系列の動的Canvasを実表示。2系列のスクリーンショットで色領域を測ると、外周19,555px・内周19,464pxで差は約0.5%（境界線と画像圧縮を含む）。
- 390pxの動的表示で横はみ出しなし。静的WebP、JavaScript無効時のSVGと系列凡例、ライト・ダークの3系列表示を確認。

以下は過去バージョンの検証履歴です。

## 1.3.11（2026-09-24）

- Excelで同じ2系列・3項目のデータから円グラフとドーナツグラフを開き、円グラフは単一系列、2系列のドーナツは外内の同心円になることを実画面で確認。
- cybernode.localに比較記事を作成。2系列・2項目と2系列・3項目のグラフを掲載。既存テスト記事も2系列・3項目であり、「3系列」は項目A・B・Cとの混同と判明。2・3項目とも同心円を表示。
- PCの動的円グラフで外側A=10/30=33.3%、内側A=7/19=36.8%を実ブラウザーで確認。中央の穴ではツールチップが消える。項目Bを非表示にした場合は表示中の合計を分母にすることを計算テストで確認。
- 390px幅では円にポインターを置いてもツールチップが出ず、ライト・ダークとも横はみ出しなし。ライトの実表示はテーマのスクリプトを除いた同一HTMLの検証用複製で確認。

以下は過去バージョンの検証履歴です。

## 1.3.10（2026-09-24）

- cybernode.local のテスト記事で、JavaScriptを除いたHTMLを実ブラウザーで表示。初期SVGと系列・項目の凡例をデスクトップと390px幅で確認。ライト・ダークの390px幅で横はみ出しなし。
- 通常表示ではChart.jsのCanvasを確認。棒→折れ線→円の切り替え後も系列・項目の非表示状態を維持。静的表示はWebP画像を生成し、動的へ戻せることを確認。
- 390px幅の通常表示で凡例メニューが閉じた状態となり、円グラフと後続グラフが表示され、横はみ出しなし。
- `php -l`、`node --check`、ZIP整合性を確認。

以下は過去バージョンの検証履歴です。

## 1.3.9（2026-09-23）

- cybernode.local の2系列テスト記事で、中心を空けたリング表示と細い系列境界を確認。系列名・項目を小さな行凡例で表示。
- 動的Canvasと静的WebPで同じ系列配置を確認。系列を非表示にし、棒グラフを経由して戻しても選択状態と静的モードを維持。
- 390px幅で凡例メニューとリングが表示され、横はみ出しなし。ダークモードで確認。ライトモードの今回の実ブラウザー再確認は未実施。
- 初期HTMLに同心円のSVGと系列順の凡例が含まれることを確認。JavaScriptを無効にした実ブラウザー表示は未確認。
- `php -l`、`node --check`、ZIP整合性を確認。

以下は過去バージョンの検証履歴です。

## 1.3.8（2026-09-23）

- cybernode.local のテスト記事で、2系列の円グラフにリング境界と外側からの系列順が表示されることを確認。
- 系列1を非表示にすると系列2が「1：系列2」となり、棒グラフを経由して円グラフに戻っても系列・項目の選択状態が保持されることを確認。
- 静的切り替えでWebPを生成。390px幅で凡例メニューの操作領域44px以上、ページ横はみ出しなし。
- 初期HTMLにSVGと系列順の凡例が含まれることを確認。JavaScriptを無効にした実ブラウザー表示は未確認。
- ダークモードでの実表示と描画エラーなしを確認。ライトモードの今回の実ブラウザー再確認は未実施。
- `php -l`、`node --check`、ZIP整合性を確認。

以下は過去バージョンの検証履歴です。

## 1.3.7（2026-09-23）

- cybernode.local のテスト記事で、棒・折れ線・円の初期形式が動的Canvasとして表示されることを確認。動的領域にSVGは生成されない。
- 円グラフの項目を非表示にした後、静的WebPへ切り替え、形式を変更して戻しても項目の選択状態が保持されることを確認。
- 390px幅で横はみ出しなし。テーマ切り替え後もCanvasが描画され、エラー表示なし。
- 初期HTMLの静的SVGは維持し、JavaScriptが動かない場合のグラフとして出力されることを確認。
- `php -l`、`node --check`、ZIP整合性を確認。

以下は過去バージョンの検証履歴です。

## 1.3.6（2026-09-23）

- cybernode.local のテスト記事で、円グラフを先頭に配置し、3つのグラフ高を260pxに設定。
- 複数系列の円グラフが間隔のある同心円として描画されることを、動的SVGと静的WebPで確認。初期HTMLのSVGも同じ描画幅を持つことを確認。
- 公開した3グラフすべてに `data-luna-copy-protected="true"` が付くこと、コピー抑止用CSSとJavaScriptが同属性に結び付くことを確認。
- 390px幅で円グラフが表示され、横はみ出しがないことを確認。静的画像はWebP、400×260px。
- `php -l`（プラグイン本体・静的SVG）と `node --check`（公開SVG・操作スクリプト）に合格。

以下は過去バージョンの検証履歴です。

## 1.3.5（2026-09-23）

- cybernode.local にLocal設置版を基にしたプラグインを反映。既存属性の省略時は棒・折れ線・円を許可すること、許可形式が1／2／3種類のブロックで初期形式と公開ボタンが一致することをPHPのレンダー結果と公開ページで確認。
- 公開ページで動的グラフがSVGとなり、Canvasを生成しないことを確認。静的切り替え後はWebP画像が表示されることを確認。
- 形式ボタンと表示モードがラジオグループとして読み上げられ、矢印キーで形式を変更すると選択状態とタブ順が更新されることを確認。
- 390px幅で横スクロールなし、ダークモードでSVGの文字色が切り替わることを確認。初期HTMLのSVGフォールバックはPHPレンダー結果で確認。
- `php -l` と `node --check`（editor.js／view.js／svg.js）に合格。
- 編集画面の設定表示は確認。保存後の再編集と、JavaScriptを無効にした実ブラウザー表示は未確認。

以下は1.3.4時点の履歴であり、1.3.5の現行動作を示すものではありません。

このZIPは独立したWordPressプラグインです。テーマ1.3.4のファイルを含まず、テーマを変更しません。

## 確認済み（1.3.4 初期）

- JavaScript構文検査。
- CSVの引用符・カンマ・改行・BOM、欠損値と0の区別、不正数値と上限超過の拒否。
- 系列・項目・文字列・日付・数値範囲の複合フィルターと元データの保持。
- 同梱SheetJS 0.20.3によるExcel書き出し・読み込みの往復。
- PHP構文、出力エスケープ、棒・折れ線・円グラフのSVG生成（欠損・0・負数・大きな数値）。
- Chromiumで動的／静的ビュー切り替え、WebP生成、PNGフォールバック。
- Chromiumで複数グラフの独立操作、絞り込み解除、データ表との同期。
- 公開領域のコピー抑止と入力欄のコピー許可。
- 390px幅とダークモード、JavaScript無効時のSVGとデータ表。
- WordPress 6.8 / PHP 8.1（WordPress Playground・SQLite）で有効化、ブロック登録、編集画面挿入、公開ページでのスクリプト読み込みとWebP表示。
- 同環境で小数の直接編集、不正入力時の保存ロックと解除、行追加、CSV取り込み、投稿保存と再読み込みでのデータ保持。

## 表示刷新後に再確認（2026-09-23）

- `core.js` / `view.js` / `editor.js` の構文検査。
- 32日・3系列のCSV。引用符内カンマ、0、負数、小数、空欄、N/A、桁区切り、突出値。元配列はフィルター後も不変。
- 日付範囲、数値範囲、系列の空選択。系列色は元の番号を維持。
- 縦軸の下限・上限をChart.jsのscaleへ渡し、データ配列自体は変更しない。
- 円グラフは負数を描画値から外し、元値は `sourceData` に残す。
- 同梱SheetJS 0.20.3で32行×3系列の書き出しと読み戻し。欠損・0・負数・突出値を維持。
- PHPの波括弧対応と、代替構文の if / endif、foreach / endforeach の対応。PHP実行環境は今回の作業環境に無いため `php -l` は未実施。
- Chromium（1280 / 768 / 390）で初期表示、系列チップの解除と復帰、日付絞り込み、縦軸上限、データ表の欠損「—」と0と負数、リセット、棒・折れ線・円、動的・静的（WebP）、ダーク（`data-theme="dark"`）、横スクロール無し、チップのタップ高44px以上。コンソールエラーなし。
- JavaScript無効の390pxで、SVG・フォールバック凡例・データ表が表示され、詳細設定は出ない。
- `--md-sys-color-*` をページに定義すると、カード背景・描画面・文字色がフォールバックより優先される。

## 導入先で未確認の範囲

- 実WordPressでのブロック挿入、再編集、保存、既存記事の再表示。属性名と `save: () => null` は維持しているが、今回の作業環境ではWordPressを起動していない。
- ブロックエディター画面の見た目。`editor.js` は構文確認のみ。
- テーマ1.3.4を同時に有効化した実ページ、キャッシュ・最適化プラグイン、独自CSP。
- Safari / Firefox 実機。WebP非対応時はブラウザー標準のPNGフォールバック。
- PHP 8.1での実行時警告。

コピー抑止は取得防止を保証しません。元データは公開HTMLに含まれます。
